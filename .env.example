### Example client .env for production when frontend and backend are hosted separately
# Set the full backend URL for API requests. When left blank, the client uses same-origin (/api/*).
VITE_API_BASE=https://learning-capcha-backend.vercel.app
